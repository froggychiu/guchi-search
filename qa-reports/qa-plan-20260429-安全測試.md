# QA Plan: 新資料庫 — 第二輪 安全測試（SQLi / XSS / Auth / Headers）

**測試標的**：
- Frontend: https://sear.newfolderla.com
- Backend: https://backend-production-b729.up.railway.app
- 程式碼：`/Users/weichiehchiu/Claude Apps/guchi-search/`

**測試日期**：2026-04-29
**測試方式**：純 read-only — 代碼審查 + production GET-only 探測 + 受控前端 DOM 注入（攔截 fetch，無寫入後端）
**測試員**：QA 安全測試員（subagent）
**範圍**：SQL/Filter injection、XSS / HTML injection、認證強度、HTTP headers、資訊洩漏、CORS
**不測試**（紅線）：
- POST 到 `/api/corrections`（會寫入 DB）
- POST 到 `/api/replace-text`、`/api/maintenance/*`、`/api/ingest`、`/api/reindex`、`/api/corrections/{id}/approve|reject`、`/api/corrections/batch-approve`（會破壞資料）
- 嘗試以已知 secret 登入 admin
- 並發 / DoS payload / 大檔上傳

---

## Feature Map（攻擊面）

- 公開 GET endpoints
  - `/api/search?q=&show=&page=&per_page=`
  - `/api/text-count?q=`
  - `/api/episodes?show=&sort=&page=&per_page=`
  - `/api/episodes/{id}`
  - `/api/popular-keywords?days=&limit=`
  - `/api/corrections/contributors?limit=`
  - `/api/corrections/verify-secret?secret=`（auth probe）
  - `/api/shows`、`/api/stats`、`/health`
- 公開 POST endpoint（不打但會審查代碼）
  - `/api/corrections`（無 auth，公開校對提交）
- 受保護 POST endpoints（不打但會看 auth 設計）
  - `/api/replace-text`、`/api/ingest`、`/api/reindex`、`/api/maintenance/{action}`
  - `/api/corrections/{id}/approve|reject`、`/api/corrections/batch-approve`
- Frontend 路由
  - `/`、`/episode/[id]`、`/admin`、`/contributors`

---

## 測試矩陣

| ID | 類型 | 案例 | 預期 | 工具 | 優先級 | 狀態 |
|---|---|---|---|---|---|---|
| **代碼審查（Read-only）** | | | | | | |
| C01 | SQLi | grep `text(`/raw SQL/ f-string + SQL | 無 raw SQL | grep | P0 | 完成 |
| C02 | SQLi (ORM) | 確認 `.contains()`、`.where()` 用 bind params | 全部安全 | Read | P0 | 完成 |
| C03 | Filter injection | Meilisearch filter 是否 escape | `f'show = "{show}"'` 直接拼接 → ❌ | Read | P0 | 完成 |
| C04 | XSS Sink | grep `dangerouslySetInnerHTML` 全部用法 | 至少審查 highlighted_text | grep | P0 | 完成 |
| C05 | Auth | 比對 secret 用 `==` 還是 `compare_digest` | 全部用 `!=` | Read | P1 | 完成 |
| C06 | Auth | secret 是否走 URL query param | `verify-secret` 確實如此 | Read | P1 | 完成 |
| C07 | Input validation | Pydantic model 的 max_length / 範圍 | corrections 缺 max_length | Read | P2 | 完成 |
| C08 | RSS | feedparser 的輸入是否 sanitize | 寫入 DB raw（HTML 在 description） | Read | P2 | 完成 |
| C09 | CORS | 比對設定 vs 實際 header | `cors_origins=["*"]` + credentials=True | Read | P1 | 完成 |
| C10 | Subprocess | replace-text 走 subprocess list 形式 | 安全（list-form，非 shell） | Read | P2 | 完成 |
| **Production GET 探測** | | | | | | |
| L01 | SQLi | `/api/search?q='` `"` `;--` `UNION SELECT` | 全 200，無錯誤洩漏 | curl | P0 | 完成 |
| L02 | Filter inj | `/api/search?show="%20OR%201=1` | ❌ Meilisearch 錯誤訊息揭露內部 attribute | curl | P0 | 完成 |
| L03 | Filter inj | `show="X" OR episode_id=1 OR show="Y"` | ❌ 成功改寫 filter | curl | P0 | 完成 |
| L04 | Null byte | `/api/text-count?q=hi\x00` | ❌ 500 Internal Server Error | curl | P1 | 完成 |
| L05 | Path traversal | `/api/episodes/../1`、`%2E%2E` | 404（FastAPI 拒絕） | curl | P2 | 完成 |
| L06 | ID edge | `/api/episodes/0`、`-1`、`abc` | 404 / 422 OK | curl | P2 | 完成 |
| L07 | Pagination | page=999999、per_page=10000 | Pydantic 422，page=999999 → 200 空清單 | curl | P2 | 完成 |
| L08 | Auth bypass | `/api/maintenance/reindex` 無 secret | 403 ✓ | curl POST 無 body | P0 | 完成 |
| L09 | Info leak | `/docs`、`/redoc`、`/openapi.json` | ❌ 全部 200 公開 | curl | P0 | 完成 |
| L10 | Info leak | `/.env`、`/.git/config` | 404 ✓ | curl | P2 | 完成 |
| L11 | CORS | OPTIONS preflight `Origin: evil.example.com` | ❌ 反射 origin + credentials=true | curl | P0 | 完成 |
| L12 | CORS | Simple GET `Origin: evil.example.com` | ❌ ACAO=*  ACAC=true（瀏覽器擋但是錯設） | curl | P0 | 完成 |
| L13 | Headers | HSTS、CSP、X-Frame-Options 等 | ❌ 全部缺 | curl -I | P1 | 完成 |
| L14 | Source map | `/_next/static/chunks/*.js.map` | 404 ✓ | curl | P2 | 完成 |
| L15 | XSS reflected | `?q=<script>` 在首頁 URL | React 自動 escape，安全 ✓ | curl | P1 | 完成 |
| L16 | URL hash | `#seg-<script>` 在 episode 頁 | regex 拒絕，安全 ✓ | Playwright | P1 | 完成 |
| **前端 DOM 注入（受控、無 DB 寫入）** | | | | | | |
| F01 | XSS sink | 攔截 fetch 注入 `<img onerror>` 到 highlighted_text | ❌ XSS 觸發（document.title='XSS'） | Playwright + fetch override | P0 | 完成 |
| F02 | Stored XSS gadget | 確認 `dangerouslySetInnerHTML` 路徑可達 | ❌ 完整鏈（POST /api/corrections → admin approve → re-index → render） | Read | P0 | 完成（gadget 確認） |

---

## 不在此次範圍

- POST 注入測試（`/api/corrections`、`/api/replace-text`、`/api/maintenance/*`、`/api/corrections/*/approve` 等）— 需 staging 環境
- Admin 已登入態下的 CSRF / token 行為 — 紅線禁止登入
- 並發、rate limit、DoS payload — 紅線
- 跨瀏覽器 / 真機網路條件
- 第三方依賴的 CVE 掃描（feedparser、meilisearch python client、openai sdk 等版本是否有已知漏洞）

---

## 收手條件
- 所有 P0/P1 案例跑完
- 連續 3 個 case 沒新發現
- 已寫完 dev bug report
